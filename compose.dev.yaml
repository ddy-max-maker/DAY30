services:
  backend:
    volumes:
      - ./app:/app/app
      - ./logs:/app/logs


    environment:

        APP_ENV: dev

    command:
      [
        "python",
        "-m",
        "uvicorn",
        "app.main:app",
        "--host",
        "0.0.0.0",
        "--port",
        "8000",
        "--reload"
      ]
    
    